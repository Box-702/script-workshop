# DESIGN.md

## Theme
暗色,唯一主题。**Codex 风格**:近黑中性分层、白色主按钮、极简单色。
场景:创作者深夜在暗光房间长时间面对屏幕改稿,屏幕是主要光源。
中性色为暖调近黑(hue 75°、chroma ≤0.006 的微染),不偏蓝不偏彩。

## Color(OKLCH)
**单色 + 语义色**策略(Codex 的核心做法):中性灰阶承担全部界面,
饱和色只留给"新增/修改/删除、成功/警告/危险"这类状态含义。
- 表面:--bg ≈#0d0d0d < --panel ≈#171717 < --panel2 ≈#1f1f1f,边框 1px --line ≈#2f2f2f 分层
- 文字:--ink ≈#ececec 主 / --muted 次 / --dim 弱
- 主操作:**白底黑字按钮**(--accent 近白 + --on-accent 近黑),这是 OpenAI/Codex 的标志样式
- 选中态:中性灰填充 --select(激活 tab、当前对话),不用彩色
- 语义:--ok ≈#10a37f 绿(成功/新增/对白) · --warn 琥珀(警告/修改) · --bad 红(危险/删除)
- diff 行底色:--diff-add-bg ≈#213A2B / --diff-del-bg ≈#4A221D(取自 Codex TUI 官方源码,
  "淡到不干扰语法色"是官方注释原话)
- 节拍:动作=中性、对白=绿、提示=弱灰(收敛进单色系)
- 悬停/按压态用 color-mix 从基色派生,不单设色值

## Typography
系统字体栈(产品工具,原生感优先)。固定像素阶梯,比率 ≈1.15:
11 / 12 / 12.5 / 13 / 14 / 16 / 18。正文 14px;聊天与剧本全文行长 ≤ 75ch;数字 tabular-nums。
**技术字符串(run id、版本号、diff 路径)用等宽字体**(.mono 类)。

## Layout
三栏结构:260px 项目树 | 弹性对话流(内容列 ≤ 820px)| 320px 查看面板。
4px 间距基准;面板内边距 12/16;分区间留白大于元素间距。

## Elevation
暗色下用「底色亮一层」表达层级,不投影;浮层(回到最新按钮、弹窗)允许极轻投影。
圆角偏大(按钮 10、卡片 14、弹窗 16),对应 Codex「每个边缘都被软化」的质感。

## Motion
140–180ms,ease-out(cubic-bezier(.25,1,.5,1))。只表达状态:悬停、焦点、开关、流式。
不做装饰动画、不做页面加载编排;尊重 prefers-reduced-motion。
弹窗不用 Vue Transition(其元素移除依赖 rAF,后台标签页冻结时会卡住关不掉),
入场用纯 CSS 动画,关闭与 v-if 同步瞬时移除。

## Components
- 按钮:primary(**白底黑字**)/ ok(绿)/ danger(红)/ ghost(描边);small 变体;全态齐备
- 输入:focus 时近白边 + 柔光环;composer 容器 focus-within 整体高亮
- 卡片:--panel 底 + 1px --line,圆角 14
- 空态:教学式文案 + 可点击建议,不放「暂无数据」死文案
- 中文输入法兼容:组词回车不触发发送
