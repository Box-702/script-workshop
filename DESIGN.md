# DESIGN.md

## Theme
暗色,唯一主题。**「钨丝灯下的稿纸」**:剧本工坊的独有身份,
从 Codex 的中性灰演进而来——保留近黑分层与浅色主按钮的骨架,
但整体浸在暖调里,并用一个克制的品牌金点亮品牌时刻。
场景:创作者深夜在暖色台灯下改稿,屏幕是唯一光源,
屏幕上是一叠泛着微光的稿纸。

## Color(OKLCH)
**暖中性 + 稿纸米白 + 品牌金 + 语义色**策略:
中性承担全部界面;米白是主操作;金只属于品牌时刻(占比刻意 <10%);
饱和语义色只留给状态含义。
- 表面(钨丝灯暖黑,hue 80 微染):--bg ≈L0.17 < --panel ≈L0.215 < --panel2 ≈L0.255,
  边框 1px --line 分层
- 文字:--ink 暖纸白主 / --muted 次 / --dim 弱(全部 hue 85 微暖)
- 主操作:**稿纸米白按钮**(--accent ≈L0.95 微暖 + --on-accent 近黑),
  保留 Codex 白按钮基因,但纸感微暖、不再纯白
- 品牌金:--gold ≈oklch(0.80 0.075 90)(聚光灯/钨丝灯)。
  只用于:开始页徽记、卡片悬停图标、Agent 工作中的流式呼吸灯、
  文本选区(::selection)。不参与正文层级、不做大面积铺色
- 选中态:中性暖灰填充 --select(激活 tab、当前对话),不用彩色
- 语义:--ok ≈#10a37f 绿(成功/新增/对白) · --warn 偏橙(警告/修改,
  hue 60,与品牌金 hue 90 分离) · --bad 红(危险/删除)
- diff 行底色:--diff-add-bg ≈#213A2B / --diff-del-bg ≈#4A221D(取自 Codex TUI 官方源码,
  "淡到不干扰语法色"是官方注释原话)
- 节拍:动作=中性暖、对白=绿、提示=弱暖灰(收敛进暖色系)
- 悬停/按压态用 color-mix 从基色派生,不单设色值

## Typography
系统字体栈(产品工具,原生感优先)。固定像素阶梯,比率 ≈1.15:
11 / 12 / 12.5 / 13 / 14 / 16 / 18(开始页标语 20px 是唯一的 hero 例外)。
正文 14px;聊天与剧本全文行长 ≤ 75ch;数字 tabular-nums。
**技术字符串(run id、版本号、diff 路径)用等宽字体**(.mono 类)。

## Layout
三栏结构:260px 项目树 | 弹性对话流(内容列 ≤ 820px)| 360px 查看面板。
4px 间距基准;面板内边距 12/16;分区间留白大于元素间距。
栏间分隔条平时弱化(半透明细痕),悬停/拖动时才亮起。

## Elevation
暗色下用「底色亮一层」表达层级,不投影;浮层(回到最新按钮、弹窗)允许极轻投影。
圆角偏大(按钮 10、卡片 14、弹窗 16),对应 Codex「每个边缘都被软化」的质感。

## Motion
140–180ms,ease-out(cubic-bezier(.25,1,.5,1))。只表达状态:悬停、焦点、开关、流式。
不做装饰动画、不做页面加载编排;尊重 prefers-reduced-motion。
弹窗不用 Vue Transition(其元素移除依赖 rAF,后台标签页冻结时会卡住关不掉),
入场用纯 CSS 动画,关闭与 v-if 同步瞬时移除。

## Components
- 按钮:primary(**稿纸米白底黑字**)/ ok(绿)/ danger(红)/ ghost(描边);small 变体;全态齐备
- 输入:focus 时近白边 + 柔光环;composer 容器 focus-within 整体高亮
- 卡片:--panel 底 + 1px --line,圆角 14;开始页建议卡悬停时图标点亮为品牌金
- 空态:品牌化开始页(LogoMark 场记板徽记 + 自适应标语 + 建议卡片),
  卡片点击填入输入框由用户确认发送
- 中文输入法兼容:组词回车不触发发送
