// =====================================================================
// main.js —— 应用入口
//
// 职责：
//   - 创建 Vue 应用实例并挂载到 #app；
//   - 引入全局样式（设计变量 + 基础控件样式）。
// =====================================================================

import { createApp } from 'vue'
import App from './App.vue'
import './style.css'

createApp(App).mount('#app')
