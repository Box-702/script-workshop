# 剧本智能体 · 面试讲稿（业务逻辑 + 实现）

> 本文用于把「剧本智能体」这个项目的**业务逻辑**讲清楚，供面试准备。
> 面向「这个项目是干什么的、为什么这么做、怎么实现的」三个问题分层展开。

---

## 0. 一句话说清

这是一个**剧本改编 Agent 工作台**：把一部小说/故事改写成结构化剧本时，AI 不是一次性整篇覆盖，而是**每次只给出一份可逐条审阅、逐条接受、可回滚的修改建议**，最终落版与否由作者决定。

---

## 1. 业务背景 / 为什么做

市面上常见的剧本/小说改编工具大多是一个「**一次生成器**」：输入原著 → 吐一份剧本 → 结束。它有三个硬伤：

1. **生成的剧本永远还要人继续改**，但工具不提供安全修改的手段；
2. **AI 全量覆盖会毁掉作者已有的结构**（人物、场景、节拍都可能被改乱）；
3. **没有版本、没有回滚、说不清「这条改动到底改了什么」**，无法协作和复盘。

所以这个项目把重心从「AI 一次生成」转移到「**AI 如何安全、可控地参与改写**」。这也是 Agent 的核心命题：**AI 提建议，人来拍板，改动必须可控、可解释、可回滚。**

---

## 2. 面向谁 / 使用场景

- **用户**：编剧、改稿人、短剧/系列剧的改编者；也适合给需要「把小说快速出成短剧剧本」的内容团队做原型。
- **产品形态**：**对话式工作台**（类似 Codex / DSH 的交互）——用户用自然语言完成整套流程，而不是填表单。
- **输入**：原著原文（`.txt / .md / .docx`）或粘贴文本 + 改编类型（短剧 / 电影 / 剧集 / 舞台剧）。
- **输出**：结构化剧本 `Script`（人物 / 地点 / 场景 / 节拍），一个剧本有**多个版本**，版本之间有 diff。

---

## 3. 业务主流程（用户视角）

```
① 新建项目：填剧名、选改编类型、提供原著原文
        │
② 生成初稿：Agent 把原著转成结构化剧本（故事圣经 → 场景/节拍）
        │
③ 提出改编需求：用一句话表达，例如「把对白改口语一点」「节奏改紧凑」
        │
④ Agent 跑审阅工作流 → 产出「审阅卡片」：结构化 patch 提议
        │
⑤ 用户在卡片上做决定（四个选择）：
        │   · 接受全部
        │   · 勾选接受部分（可只接受某几条改动）
        │   · 编辑后接受（先人工改 patch 再落版）
        │   · 重新生成（带一句反馈，让 Agent 重做）
        │   · 拒绝（剧本保持不变）
        ▼
⑥ 接受 → 生成新版本（落版，记版本 & diff）
   重新生成 → 带反馈回到提议，重跑
   拒绝 → 结束，剧本不动
        │
⑦ 反复 ③~⑥ 打磨 → 导出 .txt / .md / .docx / 版本管理 / 写入工作目录
```

每一步用户都可以用自己的语言下达指令，Agent 把「改哪里、怎么改」组织成**结构化的逐条建议**给用户看，而不是直接改完给你。

---

## 4. 为什么用 LangGraph（业务功能 ↔ 技术选型）

| 业务诉求 | 对应的技术手段 |
|---|---|
| 把「AI 建议 + 用户决定」建模成流程 | **StateGraph + 条件边**：模型是否用工具、用户是否接受，都用边来路由 |
| 落版前停下来问人 | **`interrupt`**：在 `review` 节点暂停，`Command.resume` 恢复 |
| 跨请求 / 跨重启恢复中断现场 | **checkpointer**（内存 / Postgres） |
| 少把低级错误丢给用户 | **`guard` 自纠错节点**：先 dry-run 应用、校验，有问题自己重做 |
| 建议可逐条审、可回滚 | **结构化输出 + patch 引擎**：Atom 化的 add / set / remove 操作 |
| 让回答有依据、贴合作者风格 | **项目级知识 RAG**（同类走向 / 写作手法 / 作者风格） |
| 看得到 Agent 到底做了什么 | **`graph.stream` 节点轨迹 + SSE 流式事件** |

---

## 5. 核心技术亮点与讲法

### 5.1 结构化 patch：不整场改写，只给操作
- Agent 生成本上是一条 `PatchProposal`（计划 + 逐场景改动）。
- 经 `build_patch` 规整成**一条条原子操作**（`add / set / remove`），路径是稳定可寻址的 JSON 指针，例如 `/script/scenes/0/beats/beat_002`。
- 每条操作带 `before / after`（前端可做字段级 diff）和 `risk`（如「会删除一个节拍」「会改变对白说话人」）用于高亮。
- 好处：**逐条可审、逐条可接受、可回滚**；也保留了作者原有结构（Agent 只能改目标场景的字段，不能新增人物/场景、不能乱改节拍 id）。

### 5.2 人机协同：四种决策
- `accept`（可只接受部分，`patch_indexes`）、`edit`（先人工改再接受）、`regenerate`（带反馈重做）、`reject`。
- 落地是关键：**落版是作者的决定，Agent 只负责把建议做出来**。所以审阅动作走**确定性路径**，不经过 LLM（避免模型自作主张）。

### 5.3 自我审阅 guard
- 在交给人类前，`guard` 节点先用当前 patch **dry-run 应用一遍**，再做 `validate_script` 校验。
- 只放行「error 级别问题为 0」的提议；有问题则写回 `critique`，回到 `propose` 重做（最多 `MAX_PROPOSE_ITERATIONS = 3` 次）。
- 相当于「**先自纠错，再交给人类**」，减少低级引用错误。

### 5.4 项目级知识 RAG
- 每个项目维护三类记忆：`plot_direction`（同类剧本可能的走向）、`technique`（写作手法/技巧）、`author_style`（作者的语言风格）。
- `remember` 工具收录作者偏好；`ask / run_adaptation` 检索这些知识，让改写和回答「有依据、贴合口味」。
- 实现上向量后端可插拔：**Milvus 不可达时自动退化为内存向量 + 哈希嵌入**，因此没配任何外部服务也能跑通机制。

### 5.5 持久化 + 可观测
- **Postgres** 存业务数据（项目 / 版本 / 对话 / Agent 运行记录），checkpointer 存图执行到哪一步。
- `graph.stream` 采集节点级执行轨迹，前端 SSE 把「工具调用 / 结果 / 逐字回复」推给用户，交互像对话一样。

### 5.6 兜底路径（这是关键加分点）
- **没配模型**也能演示：`fallback_patch` 把用户需求**如实地记录**进目标场景的改编说明，而不是伪造正文（诚实、不编造）。
- 数据库用 SQLite、checkpointer 用内存、向量用内存+哈希嵌入——**整条链路零外部依赖可跑通**，给人一眼就能复现。

---

## 6. 关键设计决策（面试官常问的「为什么」）

**Q1：为什么 Agent 不直接整篇改，而要用 patch？**
改剧本是「破坏性」的高风险动作。用一个或多个原子操作（而不是覆盖整份文档）能保证：可逐条审、可只接受一部分、可回滚、且不会破坏人物/场景/节拍结构。这是「AI 安全参与改写」的落点。

**Q2：为什么用 interrupt 让人决定，而不让 LLM 自己直接落版？**
落版是作者的创作决定，不是模型该替用户拍的板。所以做到 `review` 就中断，把提议交给用户；用户决定后 `Command.resume` 恢复。审阅动作全程走确定性代码，保证「接受/拒绝/重做」是稳定、可预期、可测试的。

**Q3：状态里放什么、不放什么？**
状态（`AgentState`）只放**可序列化数据**（项目 id、上下文、消息、提议、决策等），不放数据库会话、模型对象等不可序列化实例——因为状态要被 checkpointer 持久化/恢复。运行所需的 `store / llm / 工具 / 检索器` 通过**闭包注入**各节点，保证状态干净、可存档、可恢复。

**Q4：checkpointer 为什么是模块级单例？**
中断的线程状态存在 checkpointer 里，`invoke` 和 `resume` 必须命中**同一个**实例才能找回现场。若每次请求新建 `InMemorySaver`，断点信息就丢了，复审无法恢复。

**Q5：图为什么按 base 版本缓存编译？**
工具与节点通过闭包捕获「本次运行的目标剧本」，所以按 base 版本构建；同一 base 的多次运行复用同一张编译图，保证 `invoke / resume` 用同一 `thread_id` 恢复。

**Q6：没有模型 / 没有数据库怎么办？**
模型：`fallback_patch` 诚实记录需求；数据库：SQLite；checkpointer：内存；向量：内存+哈希嵌入。这条「降级路径」保证项目零配置也能跑通，也让接入模型的改动被隔离在 `llm` / `vector` 层。

---

## 7. 前端 / 交互（如果是面前端岗）

- **Vue3 单页三栏**：项目树 / 对话流 / 查看面板。
- **SSE 流式**：`fetch + ReadableStream` 逐字输出，先推工具轨迹（`tool_call / tool_result`）再流式正文。
- **审阅卡片**：把 agent 的 patch 提议做成卡片，可**勾选部分接受 / 接受全部 / 编辑 / 重新生成 / 拒绝**。
- **版本 diff**：字段级对比（before / after），高亮高风险改动（删节拍 / 改说话人）。
- **编剧设定面板**：人物小传 / 时间线 / 伏笔清单，自动写入工作目录。
- **本地文件浏览**：直接查看 / 下载 `data/<剧名>/01_原稿 / 02_版本 / 03_导出 / 04_知识库` 下的文件。

---

## 8. 可能被追问的技术细节（备查）

- ReAct 工具循环上限：`MAX_TOOL_STEPS = 8`，`tools_condition` 判断是否继续调用工具。
- 结构化输出：`llm.structured(PatchProposal)`（`with_structured_output`），产出 Pydantic 提议。
- **id 稳定性**：`normalize_id` 把 LLM 给的 `scene_1` 规整为 `scene_001`，节拍 id 保持稳定，保证 diff / 应用 / 校验可寻址。
- **校验规则**（`validate_script`）：场景/节拍 id 不重复、引用的地点/人物必须存在、对白说话人是已有角色、关键字段非空。error 级问题必须清零才能落版。
- **兼容层**：对白 `speaker` 必须引用场景已有角色（`char_xxx`）；`beats` 为主结构，`action / dialogue` 为兼容字段，应用后自动同步。
- **对话层**：上层 `ChatConductor`（绑工具的小图），底层复用改编图；`run_adaptation / resume` 工具把对话引导到审阅工作流。
- **知识库写入**：`create_project` 时 `index_project_knowledge` 会把原著拆块、抽取知识文档入库。

---

## 9. 建议的表达套路

**30 秒版（自我介绍时）**
> 我做的是一个剧本改编 Agent 工作台。市面上的改编工具基本是「一次生成」，AI 全量覆盖会毁掉作者结构、也没法回滚。我把「改编」建模成一个有状态、可中断、可回滚的 Agent 工作流：Agent 只给出**一行行原子化的修改建议**，我在卡片上逐条接受/编辑/拒绝，落版是作者的决定。底层用 LangGraph 的状态图 + interrupt/resume 做人机协同，Postgres + checkpointer 保证中断后能恢复。

**2 分钟版（问项目时）**
> 业务上它解决「AI 怎么安全地帮人改剧本」：一次生成没法安全反复改。所以核心是**结构化 patch**——Agent 产出的是 `add/set/remove` 的原子操作，每条带 before/after 和风险标记，前端做成可逐条勾选的审阅卡片。
> 技术上是 LangGraph 的一套流程：`context → plan（ReAct+工具）→ propose → guard（自纠错）→ review（interrupt 暂停）→ apply → finalize`。关键点有三：一是 `guard` 先 dry-run 应用并校验，有 error 就自己重做，最多 3 次；二是 `interrupt` + `Command.resume` 实现「停下来问人」，四种决策走确定性路径；三是项目级知识 RAG，每个项目有「同类走向/写作手法/作者风格」三类记忆，让改写贴合口味。
> 另外做了很强的可运行性兜底：没配模型就诚实记录需求，数据库可换 SQLite、checkpointer 可换内存、向量可换内存+哈希嵌入，整条链路零外部依赖能跑通。

**详细介绍版**：按第 3 节流程走一遍，再补第 5 / 6 节的设计取舍。

---

## 10. 一句话收尾

> 这个项目的本质是把「AI 一次生成」变成「AI 安全地参与改写」——用 LangGraph 把建议、审阅、落版编排成可中断、可回滚、可观测的 Agent 工作流，并把产品做成了像 Codex 一样可以直接对话使用的形态。
